[pytest]
testpaths = service/tests
python_files = test_*.py
markers =
    unit: fast tests with a fake SDK — no compiled module, no server, no room. Run with `pytest -m unit`.
    live: end-to-end tests that drive a real paired Zoom Room (starts a meeting). Opt in with ZRC_E2E_LIVE=1.
